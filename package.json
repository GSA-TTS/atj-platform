{
  "name": "10x-atj",
  "version": "1.0.0",
  "description": "Test bed for Access to Justice platform tooling.",
  "type": "module",
  "main": "index.js",
  "license": "CC0",
  "scripts": {
    "build": "turbo run build",
    "pages": "rm -rf node_modules && npm i -g pnpm turbo && pnpm i && pnpm build && ln -sf ./apps/frontend/dist _site",
    "test": "turbo run test",
    "format": "prettier --write \"packages/**/*.{js,jsx,ts,tsx}\""
  },
  "hooks": {
    "pre-commit": "pnpm format"
  },
  "devDependencies": {
    "@types/node": "^18.16.6",
    "@vitest/coverage-c8": "^0.31.0",
    "prettier": "^2.7.1",
    "ts-node": "^10.9.1",
    "tsup": "^6.4.0",
    "turbo": "^1.10.15",
    "typescript": "^5.2.2",
    "vitest": "^0.31.0",
    "vitest-mock-extended": "^1.0.7"
  },
  "dependencies": {
    "astro": "^3.3.0"
  }
}
