---
import AppFormRouter from '../../components/AppFormRouter';
import ContentLayout from '../../layouts/ContentLayout.astro';
import { getAstroAppContext } from '../../context';

const ctx = getAstroAppContext(Astro);
---

<ContentLayout title="10x Access to Justice Spotlight">
  <AppFormRouter client:only="react" uswdsRoot={ctx.uswdsRoot} />
</ContentLayout>
