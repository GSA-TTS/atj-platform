{
  "name": "@atj/documents",
  "version": "1.0.0",
  "description": "10x ATJ document generation",
  "license": "CC0",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsup src/* --env.NODE_ENV production --dts-resolve",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
